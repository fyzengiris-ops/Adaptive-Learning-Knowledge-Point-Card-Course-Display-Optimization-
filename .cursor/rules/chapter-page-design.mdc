---
description: 章节页（#chapter）已定稿 UI，改动时禁止误删/回退已确认结构与样式
globs: index.html,css/app.css,js/app.js
alwaysApply: false
---

# 章节页设计锁定（已定稿）

涉及 `view-chapter` / `#chapter` 时，默认**保留**下列已确认效果；未经用户明确要求，不要回退、删掉或用旧版结构覆盖。

## 恢复来源

定稿快照：`docs/design-snapshots/chapter-final-20260810/`  
误改后可用该目录的 `index.html` / `app.css` / `app.js` 覆盖恢复。

## 必须保留的结构

1. **顶栏标题**：`课程中心-` + 章节名（如 `UNIT 1 Teenage life`），不是单独「课程中心」。
2. **设计感在 Tab 壳**：`#chapter-tabs-shell` 上有波浪/贴纸/主题色；不要再把这套装饰挪回内容区大壳。
3. **内容区**：白底铺满（无灰底套白卡）；`#chapter-type-split` 内左右分栏（课程 | 试卷），中间竖线分隔，左右栏不再各自做独立卡片壳。
4. **栏目标题行** `.lane-head`：
   - 「课程/试卷」标签 + 短辅助文案在**同一个**左→右渐变容器里；
   - 统计用轻量 `.lane-count`（`· 4 门` / `· 2 套`）紧跟辅助文案后，不要拉到最右侧，也不要用重色胶囊。
5. **课程卡**：默认可见边框区分课程（平板无 hover）；不要只依赖 `:hover` 阴影。
6. **文案**：以 `js/app.js` 的 `CHAPTER_TYPE_META` 为准（拓展课程：`学有余力 · 拔高拓展`；试卷短文案四字：及时巩固 / 强化突破 / 挑战进阶）。

## 改动原则

- 微调可以，但先说明会动到哪一块。
- 若用户说「回到上一版/恢复定稿」，优先用上述快照目录恢复，不要凭记忆重做。
- 不要用更早的 `chapter-content-shell-20260810` 快照覆盖当前定稿，除非用户点名要那一版。
